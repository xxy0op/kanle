# kanle 使用 Docker Compose 时不需要手动填写密钥。
# 首次启动会自动生成并保存所有密钥和管理员初始密码：
# /var/kanle/.env.generated（包含敏感信息，请妥善保护）
#
# 管理员初始密码可通过以下命令查看：
# docker compose logs kanle
#
# Cloudflare R2 不使用环境变量配置，请启动后进入后台“云端存储”填写。
